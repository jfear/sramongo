language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install .
script:
  - cd tests && pytest
deploy:
  provider: pypi
  user: ''
  password:
    secure: ''
  server: https://test.pypi.org/legacy/
  on:
    branch: master
  skip_existing: true
